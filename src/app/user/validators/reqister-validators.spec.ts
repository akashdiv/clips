import { ReqisterValidators } from './reqister-validators';

describe('ReqisterValidators', () => {
  it('should create an instance', () => {
    expect(new ReqisterValidators()).toBeTruthy();
  });
});
